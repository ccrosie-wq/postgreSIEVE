\documentclass[sigconf]{acmart}
\settopmatter{printacmref=false} % Removes citation information below abstract
% ===== Don't touch this :) ======
\renewcommand\footnotetextcopyrightpermission[1]{} % removes footnote with conference information in first column
\renewcommand\footnotetextauthorsaddresses[1]{}
\pagestyle{plain} % removes running headers
% ================================

\usepackage{graphicx}
\usepackage{subcaption}

\captionsetup{justification=raggedright}

% for red coloring
\def\bad#1{\textcolor{red}{#1}}

\begin{document}
\title{CS561 Final Report: Implementing SIEVE in the Postgres Bufferpool}

\author{Ryan Crosier}
\affiliation{%
    \institution{Boston University}
    \country{}
}
\author{Will Garlington}
\affiliation{%
    \institution{Boston University}
    \country{}
}
\author{Jackson Gilstrap}
\affiliation{%
    \institution{Boston University}
    \country{}
}

\begin{abstract}
    The buffer cache is a key data structure in the DBMS that helps reduce unnecessary disk I/Os. Eviction policies govern the removal of stale or infrequently accessed data, making room for new entries. Some of the most common eviction algorithms are Least Recently Used (LRU), Least Frequently Used (LFU), and First In First Out (FIFO). PostgreSQL uses CLOCK Sweep \bad{citethis}, a modification of the LRU algorithm designed to minimize the overhead of maintaining an ordered list of recently used pages. For our project, we implement SIEVE, an eviction policy originally designed as an LRU alternative for web caches into PostgreSQL. SIEVE is advantageous due to its lock-free design and its ability to be easily integrated into existing cache architectures. Whilst SIEVE was originally made for the web, there may be interesting use for it in database systems such as PostgreSQL. Our experiments benchmark SIEVE against other eviction policies using \textit{pgbench}. We find that SIEVE provides little (if any) improvement in terms of throughput and cache-hit ratio over the default buffer cache eviction policy on mixed and skewed workloads.
\end{abstract}

\maketitle

% =============================================================================
\section{Introduction}
% =============================================================================

\subsection{Motivation}

The most successful cache eviction policy introduced in recent years is SIEVE \cite{SIEVE}, an algorithm which is even simpler to implement than LRU. While originally designed for web-caching workloads, it has been found to be useful in many workloads with a high degree of data skew. While the paper introducing SIEVE concedes that it may not perform well for buffer cache workloads due to the presence of range scans, many real-world workloads may have a much higher proportion of point queries for which the benefits of SIEVE can be exploited.

\subsection{Problem Statement}

The problem we explored is whether or not there are workloads for which SIEVE outperforms other simple eviction policies for the bufferpool in PostgreSQL. By default PostgreSQL uses the CLOCK algorithm for buffer cache eviction, we also implemented LRU as another point of comparison. We then explored workloads with varying data skews to determine whether SIEVE would grant performance benefits or reduce write amplification.

\subsection{Contributions}

We make the following contributions:

\begin{itemize}
    \item We implemented a framework in PostgreSQL for implementing and switching between different buffer cache eviction policies at compile time.
    \item We present results indicating that SIEVE does not offer any significant performance benefits over CLOCK in skewed and mixed workloads.
\end{itemize}


% =============================================================================
\section{Background}
% =============================================================================
For decades, cache eviction has been primarily dominated by primitive algorithms such as First In First Out (FIFO) and Least Recently Used (LRU). 
More complex algorithms such as Adaptive Replacement Cache (ARC) typically have better results by exploiting different parts of the system to account for weaknesses in the aforementioned algorithms; however require the developer to implement hundreds of lines of code. 
This persistent trade-off between ease of implementation and performance has been continuously discussed and researched in system research. As discussed in recent work \cite{SIEVE} SIEVE is a recent advancement in this space, originally designed for and evaluated against web cache workloads. 
The authors categorize SIEVE as a cache eviction primitive similar to LRU or FIFO. Structurally, it is built upon a single FIFO queue and utilizes a "moving hand" pointer to track whether data has been recently accessed. 
This hand scans from the tail toward the head of the queue, resetting the "visited" bit of objects it encounters. An object is only evicted if its visited bit is 0; otherwise, it is retained in its original position, effectively "sifting" the cache to preserve popular data. 

% =============================================================================
\section{Architecture}
% =============================================================================

\subsection{Buffer Policy Framework}

To facilitate the testing of several eviction policies in a common 'environment', we've implemented an eviction policy framework on which all policies can be built. This framework is made up of two primary components: a BufferStrategyCommon which holds a buffer lock and other policy data, and an EvictionVtable which defines virtual functions to be implemented by new policies. Because \emph{freelist.c}, where eviction logic is implemented, does not dictate when or how buffers enter or leave the buffer pool, our virtual functions can focus purely on the mechanics of evictions and insertions.

\subsection{Sieve Implementation}

Our SIEVE implementation was designed with static arrays to minimize the overhead of memory reallocation. Two static arrays sized to \emph{NBuffers}, the number of buffers allowed in the pool, combine to recreate the behavior of a linked list. Each insertion or \emph{GetBuffer} (find victim) call is treated as an atomic action where a Spinlock is held until the function completes. After every insertion or deletion, the ID of the buffer is used to access the array and either mark the buffer as the new head (insertion) or designate the buffer as unreachable (eviction).

% ================================================
\section{Benchmarking \& Results}
% =============================================================================

We have made substantial changes to our benchmarking setup since the midway report. These changes will be pointed out in the following subsections. We would also like to mention that the results from our midway report are entirely invalid because we had not been properly switching cache eviction policies due to a miscommunication. This has been resolved, and detailed information on how to test this yourself can be found in our codebase.

\subsection{Hardware}

We perform all of our evaluations on a system running Linux Mint (a Debian fork). The system has an AMD Ryzen 5 5600X CPU with 6 Cores at 3.7GHz base clock, and 32 GB of DDR4 memory. The system is equipped with two storage devices: a 256 GB SATA SSD and a 1 TB NVMe SSD.

\subsection{Postgres Setup}

Postgres data is stored on the NVMe SSD for every test, and the WAL is stored on the SATA SSD. In the midterm, the only device was the SATA SSD. Benchmarks use the standard pgbench tables with a scale factor of 1000. In our midterm results, we had erroneously used a scale factor of 1 when generating the tables. Auto-vacuuming is disabled, and the tables are vacuumed manually after each test to ensure a clean database for the next test. In our midterm results, auto-vacuuming had been enabled.

\subsection{Pgbench Workloads}

We test over various workloads with different data localities and read/write ratios. For a skewed workload, we draw points from a Zipfian distribution with $\alpha=1.01$. This is a lower parameter than we used in the midterm to get a slightly more dramatic skew.

\subsection{Results}

Our results show that in the SQL use case we tested, SIEVE does show signs of slightly improved performance for very small buffers, but this difference in performance disappears as buffer size increases.

\begin{figure}
    \includegraphics[width=0.7\linewidth]{images/uni_latency_900.png}
    \caption{Average query latency of uniform queries with a 900MB BP.}.
    \label{fig:unilatnine}
\end{figure}
\begin{figure}
    \includegraphics[width=0.7\linewidth]{images/zipf_latency_900.png}
    \caption{Average query latency of skewed Zipfian queries with a 900MB BP.}
    \label{fig:ziplatnine}
\end{figure}
\begin{figure}
    \includegraphics[width=0.7\linewidth]{images/uni_latency_1800.png}
    \caption{Average query latency of uniform queries with a 1800MB BP.}
    \label{fig:unilateight}
\end{figure}
\begin{figure}
    \includegraphics[width=0.7\linewidth]{images/zipf_latency_1800.png}
    \caption{Average query latency of skewed Zipfian queries with a 1800MB BP.}
    \label{fig:ziplateight}
\end{figure}

Figures \ref{fig:unilatnine} and \ref{fig:ziplatnine} show these results most clearly, where SIEVE outperforms CLOCK in latency across all R/W rations for the 900MB bufferpool. Despite the higher latency, CLOCK seems to maintain a slight edge in cache hit rations on all buffer sizes with write heavy workloads. 

\begin{figure}
    \includegraphics[width=0.7\linewidth]{images/uni_ratio_900.png}
    \caption{Cache hit ratio of uniform queries with a 900MB BP.}.
\end{figure}
\begin{figure}
    \includegraphics[width=0.7\linewidth]{images/zipf_ratio_900.png}
    \caption{Cache hit ratio of skewed Zipfian queries with a 900MB BP.}
\end{figure}
\begin{figure}
    \includegraphics[width=0.7\linewidth]{images/uni_ratio_1800.png}
    \caption{Cache hit ratio of uniform queries with a 1800MB BP.}
\end{figure}
\begin{figure}
    \includegraphics[width=0.7\linewidth]{images/zipf_ratio_1800.png}
    \caption{Cache hit ratio of skewed Zipfian queries with a 1800MB BP.}
\end{figure}

When measuring throughput over different read ratios in Figures \ref{fig:tputrwskew} and \ref{fig:tputrwuni}, we see little variance in performance between the policies; however, we do see that LRU falls behind the others for uniform queries. This behavior falls in line with Figure 10C of~\cite{ACE} which is the result we were trying to reproduce.

\begin{figure}
    \includegraphics[width=0.7\linewidth]{images/tputvrw900.png}
    \caption{Throughput as R/W ratio increases on skewed queries.}
    \label{fig:tputrwskew}
\end{figure}

\begin{figure}
    \includegraphics[width=0.7\linewidth]{images/tputvrw900uni.png}
    \caption{Throughput as R/W ratio increases on uniform queries.}
    \label{fig:tputrwuni}
\end{figure}

Significantly, the transaction throughput of SIEVE in our experiments exhibited sublinear scaling in-line with CLOCK and LRU (Figure~\ref{fig:sublin}), and below the linear scaling observed in Figure 6 of~\cite{SIEVE}. We have tried to closely replicate the conditions under which the original scaling was seen, but have not seen the same performance characteristics. This could be due to a number of factors, but we believe that it is likely due to the different database environments these tests were run in. The datasets used in~\cite{SIEVE} are KV-cache traces from real-world systems replayed in a test environment. Postgres is likely to have significantly different indexing structures than a KV-cache. Along with the relational schema, we believe that these differences could hide any performance benefits observed in the context of the original paper. It is also possible that our synthetic benchmarks are not comparable workload to the traces used in SIEVE. If we had more time, we would try and set up this benchmark in a Postgres environment.

\begin{figure}
    \includegraphics[width=0.7\linewidth]{images/tput_scaling.png}
    \caption{Throughput scaling as clients, threads, and memory increase. We use half as many threads as clients, and scale the bufferpool size proportionally with clients (starting at 900 MB, then 1800 MB, etc.). The workload is skewed, and the workload is mixed 1/1.}
    \label{fig:sublin}
\end{figure}

Finally, we observed in our testing setup that throughput hit a wall on when the size of the bufferpool hit about \%20 of the total data size. We expected to see something more similar to the graph in Figure 10E of~\cite{ACE}, in which the workload latency would rapidly decrease as bufferpool size increased.

To investigate, we verified that cache hit ratio was increasing, and performed the same experiment with a read-heavy workload. Again, we saw the performance gains hit a wall. As we increased the number of threads, the throughput increased, but we did not see a change in the shape of the graph.

Ultimately, we were unable to determine the cause of the bottleneck in this test. As a final effort, we disabled file syncing in postgres to try and remove all disk accesses, but this did not change the shape of our graph.


\begin{figure}
    \includegraphics[width=0.7\linewidth]{images/scaling_uniform.png}
    \caption{Performance scaling as only buffer size increases for uniform queries on a mixed workload.}
\end{figure}

\begin{figure}
    \includegraphics[width=0.7\linewidth]{images/scaling_skewed.png}
    \caption{Performance scaling as only buffer size increases for skewed queries on a mixed workload.}
\end{figure}


% =============================================================================
\section{Conclusion}
% =============================================================================

We have implemented and evaluated SIEVE against Clock Sweep for various synthetic workloads and found that there is little difference in throughput and cache hit ratio. Our next steps are to additionally measure the write amplification of different eviction policies over different workloads and determine if SIEVE is able to pull ahead in any of those scenarios. We would also like to test synthetic workloads include range scans, which should perform worse according to~\cite{SIEVE}.


% reference
{
    \bibliographystyle{ACM-Reference-Format}
    \bibliography{biblio}
} 

\end{document}

